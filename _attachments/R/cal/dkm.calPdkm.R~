dkm.calPdkm <- function(ccc){
  msg <- "calculated by  dkm.calPcal()"
  a <- abbrevList(ccc)


  ## kommt alles aus Analysis struct
  PNOM <- getSubList(a$cav$Pressure, "dkm_nom")
  pnom   <- getConstVal(NA,NA,PNOM)

print(pnom)
  return(ccc)
}
