Sys.setenv("no_proxy" = saveNoProxy)

list(c(1,2,3))
list(c(1))
list(c(1)) - 5
list(c(1)) - as.vector(5)
list(c(1)) - as.list(5)
list(c(1)) ^2
unlist(list(c(1))) -5
library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE
cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){

      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)
doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE
cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){

      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)
doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE
cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){

      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)
doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE
cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){

      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)
doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE
cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){

      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)
doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE
cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){

      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)
doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE
cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){

      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)
doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

q()
n
library(methods,  quietly =TRUE)
library(bitops,   quietly =TRUE)
library(RJSONIO,  quietly =TRUE)
library(RCurl,    quietly =TRUE)
library(R4CouchDB,quietly =TRUE)

cdb  <- cdbIni()
loadSrc <- TRUE

cdb$DBName     <- "vaclab_db" ## DBName

saveNoProxy <- Sys.getenv("no_proxy")
Sys.setenv("no_proxy" = "*")


if(loadSrc){

  cdb$id <- paste("_design/rproc",sep="")
  srcDoc <- cdbGetDoc(cdb)$res
  files <- names(srcDoc$'_attachments')

  baseSrcUrl <- paste(cdb$baseUrl(cdb),
                      cdb$DBName,"/",
                      cdb$id,"/",
                      sep="")

  for(file in  files){
    fn <- grep("^R/.*\\.R$",file)

    if(length(fn) > 0){


      srcUrl <- paste(baseSrcUrl,
                        file,
                        sep="")

        source(srcUrl)

      }
  }
}

cdb$id         <- "81639d04934132e766657369210f2629"
doc <- cdbGetDoc(cdb)$res
doc <- refreshAnalysis(cdb,doc)
doc <- getOutIndex(doc)

doc <- calT(doc)

doc <- calPfill(doc)

doc <- calDvC(doc)
doc <- calQ(doc)
doc <- calMfp(doc)
doc <- calPcal(doc)






Sys.setenv("no_proxy" = saveNoProxy)

doc$Calibration$Analysis$Values$Pressure
doc$Calibration$Analysis$Values$Temperature
q()
n
q()
n
